# Folder Logic

## Nested Navigation
To create the nested look you see in the sidebar:
1. Create a sub-folder in GitHub.
2. Add your Markdown files inside that folder.
3. Reference them in the `SUMMARY.md` with an indentation.

## Why use Folders?
* Better organization for large projects.
* Allows for logical URL paths (e.g., `/setup/logic`).
